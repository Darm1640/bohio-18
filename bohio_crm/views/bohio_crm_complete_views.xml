<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- VISTA KANBAN BOHIO CRM CON DASHBOARD Y MAPA -->
    <record id="view_crm_lead_kanban_bohio" model="ir.ui.view">
        <field name="name">crm.lead.kanban.bohio</field>
        <field name="model">crm.lead</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <kanban default_group_by="stage_id" class="o_kanban_small_column o_bohio_crm_kanban" sample="1" quick_create="false"
                    on_create="quick_create" js_class="bohio_crm_dashboard_kanban">
                <field name="stage_id"/>
                <field name="partner_id"/>
                <field name="user_id"/>
                <field name="expected_revenue"/>
                <field name="recurring_revenue"/>
                <field name="estimated_commission"/>
                <field name="probability"/>
                <field name="company_currency"/>
                <field name="email_from"/>
                <field name="phone"/>
                <field name="tag_ids"/>
                <field name="activity_ids"/>
                <field name="activity_state"/>
                <field name="project_id"/>
                <field name="compared_properties_ids"/>
                <field name="property_ids"/>
                <field name="color"/>
                <field name="client_type"/>
                <field name="service_interested"/>

                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click" t-attf-style="border-left: 4px solid #{kanban_color(record.color.raw_value)}">
                            <!-- Dropdown Menu -->
                            <div class="o_dropdown_kanban dropdown">
                                <a class="dropdown-toggle o-no-caret btn" role="button" data-bs-toggle="dropdown" href="#" aria-label="Menu" title="Menu">
                                    <span class="fa fa-ellipsis-v"/>
                                </a>
                                <div class="dropdown-menu" role="menu">
                                    <a role="menuitem" type="edit" class="dropdown-item">Editar</a>
                                    <a role="menuitem" type="delete" class="dropdown-item">Eliminar</a>
                                    <div class="dropdown-divider"/>
                                    <a role="menuitem" type="object" name="action_schedule_meeting" class="dropdown-item">
                                        <i class="fa fa-calendar me-1"/> Agendar Reunión
                                    </a>
                                    <a role="menuitem" class="dropdown-item" type="object" name="action_make_call">
                                        <i class="fa fa-phone me-1"/> Llamar
                                    </a>
                                    <a role="menuitem" class="dropdown-item" type="object" name="action_send_email">
                                        <i class="fa fa-envelope me-1"/> Enviar Email
                                    </a>
                                    <div class="dropdown-divider"/>
                                    <ul class="oe_kanban_colorpicker" data-field="color"/>
                                </div>
                            </div>

                            <!-- Título -->
                            <div class="o_kanban_record_top mb-2">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                            </div>

                            <!-- Cliente -->
                            <div class="mb-2">
                                <i class="fa fa-user text-primary me-1" title="Cliente"/>
                                <field name="partner_id"/>
                            </div>

                            <!-- Tipo y Servicio -->
                            <div class="mb-2">
                                <t t-if="record.client_type.raw_value">
                                    <span class="badge bg-info me-1">
                                        <t t-esc="record.client_type.value"/>
                                    </span>
                                </t>
                                <t t-if="record.service_interested.raw_value">
                                    <span class="badge bg-success">
                                        <t t-esc="record.service_interested.value"/>
                                    </span>
                                </t>
                            </div>

                            <!-- Contacto -->
                            <t t-if="record.email_from.raw_value">
                                <div class="text-muted small mb-1">
                                    <i class="fa fa-envelope me-1"/>
                                    <t t-esc="record.email_from.value"/>
                                </div>
                            </t>
                            <t t-if="record.phone.raw_value">
                                <div class="text-muted small mb-2">
                                    <i class="fa fa-phone me-1"/>
                                    <t t-esc="record.phone.value"/>
                                </div>
                            </t>

                            <!-- Proyecto y Propiedades -->
                            <t t-if="record.project_id.raw_value">
                                <div class="mb-1">
                                    <span class="badge bg-secondary">
                                        <i class="fa fa-building-o me-1" title="Proyecto"/>
                                        <field name="project_id"/>
                                    </span>
                                </div>
                            </t>
                            <t t-if="record.property_ids.raw_value">
                                <div class="mb-2">
                                    <field name="property_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                </div>
                            </t>

                            <!-- Métricas Financieras -->
                            <div class="o_kanban_tags_section mb-2">
                                <div class="row g-1">
                                    <div class="col-6">
                                        <div class="text-center p-1 bg-light rounded">
                                            <small class="text-muted d-block">Ingreso</small>
                                            <strong>
                                                <field name="expected_revenue" widget="monetary" options="{'currency_field': 'company_currency'}"/>
                                            </strong>
                                        </div>
                                    </div>
                                    <div class="col-6">
                                        <div class="text-center p-1 bg-light rounded">
                                            <small class="text-muted d-block">Comisión</small>
                                            <strong class="text-warning">
                                                <field name="estimated_commission" widget="monetary" options="{'currency_field': 'company_currency'}"/>
                                            </strong>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Barra de Probabilidad -->
                            <div class="mb-2">
                                <div class="d-flex justify-content-between mb-1">
                                    <small class="text-muted">Probabilidad</small>
                                    <small class="fw-bold"><t t-esc="record.probability.value"/>%</small>
                                </div>
                                <div class="progress" style="height: 6px;">
                                    <div class="progress-bar"
                                         t-attf-class="bg-#{record.probability.raw_value >= 75 ? 'success' : record.probability.raw_value >= 50 ? 'info' : record.probability.raw_value >= 25 ? 'warning' : 'danger'}"
                                         t-attf-style="width: #{record.probability.value}%">
                                    </div>
                                </div>
                            </div>

                            <!-- Tags -->
                            <div class="mb-2">
                                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </div>

                            <!-- Footer -->
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <field name="activity_ids" widget="kanban_activity"/>
                                </div>
                                <div class="oe_kanban_bottom_right">
                                    <field name="user_id" widget="many2one_avatar_user"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- VISTA LIST BOHIO CRM -->
    <record id="view_crm_lead_list_bohio" model="ir.ui.view">
        <field name="name">crm.lead.list.bohio</field>
        <field name="model">crm.lead</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <list string="Oportunidades" sample="1" multi_edit="1">
                <header>
                    <button name="action_schedule_meeting" type="object" string="Agendar Reunión" icon="fa-calendar"/>
                    <button name="action_set_won_rainbowman" type="object" string="Marcar Ganada" icon="fa-check-circle"/>
                    <button name="action_set_lost" type="object" string="Marcar Perdida" icon="fa-times-circle"/>
                </header>
                <field name="name" string="Oportunidad"/>
                <field name="partner_id" string="Cliente"/>
                <field name="client_type" string="Tipo" optional="show"/>
                <field name="service_interested" string="Servicio" optional="show"/>
                <field name="email_from" string="Email" optional="hide"/>
                <field name="phone" string="Teléfono" optional="hide"/>
                <field name="project_id" string="Proyecto" optional="show"/>
                <field name="property_ids" string="Propiedades" widget="many2many_tags" optional="show"/>
                <field name="stage_id" string="Etapa"/>
                <field name="expected_revenue" string="Ingreso Esp." widget="monetary" sum="Total" optional="show"/>
                <field name="recurring_revenue" string="Recurrente" widget="monetary" sum="Total" optional="hide"/>
                <field name="estimated_commission" string="Comisión" widget="monetary" sum="Total" optional="show"/>
                <field name="probability" string="Probabilidad" widget="progressbar" optional="show"/>
                <field name="date_deadline" string="Fecha Esperada" optional="hide"/>
                <field name="user_id" string="Responsable" widget="many2one_avatar_user" optional="show"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" optional="hide"/>
                <field name="activity_ids" widget="list_activity" optional="show"/>
                <field name="company_currency" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- VISTA CALENDAR BOHIO CRM -->
    <record id="view_crm_lead_calendar_bohio" model="ir.ui.view">
        <field name="name">crm.lead.calendar.bohio</field>
        <field name="model">crm.lead</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <calendar string="Calendario" date_start="date_deadline" color="user_id" mode="month" event_open_popup="true">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="expected_revenue" widget="monetary"/>
                <field name="user_id" filters="1" avatar_field="avatar_128"/>
                <field name="stage_id"/>
            </calendar>
        </field>
    </record>

</odoo>
