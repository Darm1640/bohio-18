<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- CLIENT ACTION: Timeline View (Original) -->
    <record id="action_crm_lead_timeline_view" model="ir.actions.client">
        <field name="name">Vista Timeline</field>
        <field name="tag">bohio_timeline_view</field>
        <field name="target">current</field>
        <field name="context">{}</field>
    </record>

    <!-- CLIENT ACTION: Timeline View V2 (Mejorada) -->
    <record id="action_crm_lead_timeline_view_v2" model="ir.actions.client">
        <field name="name">Vista Timeline V2</field>
        <field name="tag">bohio_timeline_view_v2</field>
        <field name="target">current</field>
        <field name="context">{}</field>
    </record>



    <!-- MENU ITEM: Vista Rápida CRM (Original) -->
    <menuitem id="menu_crm_timeline_view"
              name="Vista Rápida CRM"
              parent="crm.crm_menu_sales"
              action="action_crm_lead_timeline_view"
              sequence="10"/>

    <!-- MENU ITEM: Vista Timeline (Principal) -->
    <menuitem id="menu_crm_timeline_view_v2"
              name="Vista Timeline"
              parent="crm.crm_menu_sales"
              action="action_crm_lead_timeline_view_v2"
              sequence="11"/>

</odoo>
